@model BookStoretestOnMVC.Models.LoginClass

@{
    ViewBag.Title = "Dashboard";
}
    <body style="background-color:azure">
        <div class="navbar navbar-inverse navbar-fixed-top">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    @Html.ActionLink("Add Book", "Insert", "Item", new { area = "" }, new { @class = "navbar-brand" })
                    @Html.ActionLink("Desplay Record", "ShowList", "Item", new { area = "" }, new { @class = "navbar-brand" })
                    @*@Html.ActionLink("Sign Up", "Index", "UserLogin", new { area = "" }, new { @class = "navbar-brand" })
                    @Html.ActionLink("Sign In", "Login", "UserLogin", new { area = "" }, new { @class = "navbar-brand" })*@
                </div>
                <div class="navbar-collapse collapse">
                    <ul class="nav navbar-nav">
                    </ul>
                </div>
            </div>
        </div>

        @if (Session["Email_id"] != null)
        {
            <h2>Welcome @Session["Email_id"].ToString()</h2>
        }

        <h2 align="center"> Dashboard</h2>

